let numbers = [2, 5, 12, 13, 15, 18, 22];
class Car{
    //ここに答えを実装してください。↓↓↓
    constructor(gas,numbers){
        this.gas = gas;
        this.num = numbers;
    }
    //「ガソリンは〇〇です。ナンバーは△△です」と出力される「getNumGas」という関数
    getNumGas(){
        console.log(`ガソリンは${this.gas}です。ナンバーは${this.num}です。`);
    }
}
    function isEven(num) {
        return num % 2 === 0;
        console.log(num + `は偶数です`);
    }
    num.getNumGas();
